syntax = "proto3";

package project;

option go_package = "github.com/rafiulhc/grpc-blockchain-endpoints/blockchain/proto";

message BlockRequest {
    string name = 1;
}

message BlockResponse {
    string message = 1;
}

service BlockService {
    rpc Block (BlockRequest) returns (BlockResponse);
}